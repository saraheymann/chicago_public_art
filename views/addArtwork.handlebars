{{!--form to input data, we may want to put this on another page  --}}
    {{!-- <form ref='uploadForm' 
      id='uploadForm' 
      action='http://localhost:8080/upload' 
      method='post' 
      encType="multipart/form-data">
        <input type="file" name="image" />
        <input id= "formButton" type='submit' value='Upload!' />
    </form>      --}}

    {{!--on submit we need to generate a marker  --}}

<main>

    <div class="container">
        <div class="row">
            <form class="col-sm-12 col-md-12 col-lg-12">
                <div class="form-group">
                    <label for="artistName">Artist Name</label>
                    <input type="text" class="form-control" id="artistNameInput" placeholder="Enter an Artist Name">
                </div>

                <div class="form-group">
                    <label for="artworkName">Artwork Name</label>
                    <input type="text" class="form-control" id="artworkNameInput" placeholder="Enter an Artwork Name">
                </div>

                <div class="form-group">
                    <label for="location">Artist Name</label>
                    <input type="text" class="form-control" id="locationInput" placeholder="Enter an Address" required="true">
                </div>

                <div class="form-group">
                    <label for="artDescription">Artist Name</label>
                    <input type="text" class="form-control" id="artDesriptInput" rows="10" placeholder="Enter a Brief Description" required="true">
                </div>
            </form>
        </div>

        <div class="row">
            <button type="submit" id="form-submit" class="btn btn-success btn-lg">
                Submit Artwork
            </button>
        </div>
    </div>

    <script>

        var marker;

        $('#formButton').addListener('click', function(event) {
            //latLng will have to be changed to however we're grabbing that data
            placeMarker(event.latLng);
        });

        function placeMarker(location) {
            var marker = new google.maps.Marker({
                position: location, 
                map: map
        });

        //we'll need to update the info Window content string with the variables attached to the data
        var contentString = '<div id="content">'+
            '<div id="siteNotice">' +
            '</div>' + '<div style="float:left"><img src="imgVariable"></div>'
            '<h1 id="firstHeading" class="firstHeading">'NameOfArtwork'</h1>' +
            '<div id="bodyContent">' + '<p>'address'<p><br>'
            '<p>'description'</p>' +
            '</div>' +
            '</div>';
            
        var infowindow = new google.maps.InfoWindow({
            content: contentString
        });

        marker.addListener('click', function() {
            infowindow.open(map, marker);
        });

        map.setCenter(location);

    <script>

</main>